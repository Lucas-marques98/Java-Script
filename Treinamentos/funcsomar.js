function somar(y, l) {
  let resultado = y + l;
  return resultado
}

console.log(somar(10, 30))

let objeto = {
  temperatura: 50,
  umidade: 10,
  valor: 40,
}

console.log(`
Temperatura ===> ${objeto.temperatura}
umidade ===> ${objeto.umidade}
valor ===> ${objeto.valor}`)