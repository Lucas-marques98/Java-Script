let numero = 20;
let pessoa = "digite um numero ";
if (numero >= 20) { // vai checar se o numero (20) é maior que 20 
  numero = "seu numero é maior que 20"
} else {
  numero = "seu número é menor que 20"
}

console.log(`${pessoa}: ${numero}`);

//Usando somente If e Else (Importante lembrar que o Else pode ser usado sem criar uma condição)